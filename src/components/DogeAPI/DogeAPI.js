import './DogeAPI.scss';

const DogeAPI = (props) => {
  const t = props.translation;

  return (
    <div className='content'>
      DogeAPI Component
    </div>
  );
}

DogeAPI.propTypes = {};

DogeAPI.defaultProps = {};

export default DogeAPI;
